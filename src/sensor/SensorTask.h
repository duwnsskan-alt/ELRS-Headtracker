#pragma once
#include <Arduino.h>
#include "SharedState.h"

// ═══════════════════════════════════════════════════════════════
//  SensorTask  —  BNO085를 100Hz로 읽어 SharedState에 기록
// ═══════════════════════════════════════════════════════════════

class SensorTask {
public:
    // FreeRTOS 태스크 생성 (main.cpp에서 한 번 호출)
    static void start();

private:
    static void taskFn(void* arg);   // FreeRTOS 진입점
    static bool initSensor();        // BNO085 초기화
    static void readSensor();        // 한 프레임 읽기
};
